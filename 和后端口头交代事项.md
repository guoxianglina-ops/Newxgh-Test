# 需要和后端口头/文字交代的内容（不需文件传递）

> 以下内容不适合写在接口文档里，但后端必须知道。建议口头沟通或发微信。

---

## 一、交接清单告知

把 `backend-handoff` 文件夹整个发给后端开发，里面包含：

| 文件 | 说明 |
|------|------|
| `接口文档.md` | Markdown 格式（推荐） |
| `接口文档.doc` | Word 格式（可在 Office/WPS 中打开） |
| `中间人确认清单.md` | 让中间人填完再给后端 |
| `wms-core.js` | 前端核心代码（后端需要看 WMS_API 模块） |
| `index.html` | 桌面版 |
| `mobile.html` | 手机版（二维码扫码目标页） |
| `xlsx.full.min.js` | 前端依赖库 |

---

## 二、后端必须知道的几件事

### 1. 二维码地址绝对不能变

这是最重要的一点。每个二维码里写着完整的服务器地址：

```
http://服务器IP:端口/mobile.html?action=quick-in&goodsId=1&qty=10&...
```

一旦地址变了，所有已打印贴在商品上的二维码全部报废。所以：

- **优先用域名**，IP 变了改 DNS 就行
- 如果只能用 IP，必须是**固定公网 IP**
- 端口确定后就不要再改

### 2. 前端的工作模式

前端是纯静态 SPA，没有任何构建过程。后端只需要：
- 把 `index.html` / `mobile.html` / `wms-core.js` / `xlsx.full.min.js` 放在服务器的静态目录下
- 实现 `GET /api/data` 和 `POST /api/data` 两个接口，整个系统就能运作
- 把 `wms-core.js` 第 753 行（`SUPABASE_URL`）和第 754 行（`SUPABASE_KEY`）改成新后端的地址

### 3. ID 分配必须用行锁或原子操作

前端预取 ID 号段的方式：一次取 50 个 ID，在自己号段内分配，用完再取。后端必须保证同一条 key 的 ID 分配是原子的，否则多设备同时操作会 ID 冲突。

### 4. 乐观锁的合并逻辑

如果版本冲突（409），前端会自动合并数据后重试。合并逻辑在 `wms-core.js` 的 `_mergeData()` 函数（~902 行）。后端不需要操心合并，只管返回最新数据就行。

### 5. 当前前端没有真正的认证

当前账号和密码硬编码在 `wms-core.js` 的 `ACCOUNTS` 对象里（明文密码）。这是过渡方案。后端接手后建议：
- 实现 JWT 登录认证
- 密码 hash 存储
- 接管前端认证逻辑（改 `doLogin` 函数调用后端接口）

### 6. 混合模式的本地降级

前端有这个逻辑：如果 fetch 失败，会 fallback 到 localStorage，不会崩溃。后端开发阶段可以充分利用这一点——即使后端挂了，前端也能在本地模式下继续运行。

---

## 三、部署步骤（给后端参考）

1. 在服务器上装好数据库（PostgreSQL / MySQL / 等）
2. 建表：`app_data`（单表 JSONB）+ `id_counters`
3. 实现 `GET /api/data` 和 `POST /api/data`
4. 实现 `POST /api/id/allocate`
5. 把前端文件放在 HTTP 服务的静态目录下
6. 修改 `wms-core.js` 中的 `SUPABASE_URL` 为后端地址
7. 启动后端，用浏览器访问测试
8. 生成二维码，用手机扫码测试
9. 配置开机自启 + 定时备份

---

## 四、交接方式建议

### 推荐：GitHub 私有仓库

1. 创建一个 GitHub 私有仓库
2. 把整个项目（包括前端文件 + `backend-handoff`）推上去
3. 拉后端开发进仓库
4. 后端在仓库里 Wiki/Issues 问问题

**优点**：
- 所有文件版本可追溯
- 后端可以开 Issue 问问题
- 后续改 bug 有 Git 历史

### 备选：网盘/ZIP 打包

如果甲方不允许代码上 GitHub（部分企业有规定），直接 ZIP 打包 `backend-handoff` 发给后端。

---

## 五、前后端协作工作流建议

```
中间人填写「中间人确认清单.md」→ 交给后端
                              ↓
后端根据服务器信息选型数据库+框架 → 实现三组接口
                              ↓
后端部署到测试环境 → 中间人测试 → 反馈问题
                              ↓
问题修复 → 部署正式环境 → 交付甲方
```

---

## 六、常见问题预设

**Q: 后端问「能不能不用 JSONB 单表模式，我想拆成关系表」？**

A: 可以，但前端所有数据读写走 `GD()` / `saveD()` 这两个函数，操作的是整个 JSON 对象。如果拆表，后端需要额外在 `GET /api/data` 中把所有表拼回 JSON 对象返回，在 `POST /api/data` 中把 JSON 对象拆回各表写入。额外工作量不小。

**Q: 后端问「能不能改用 RESTful 单条 CRUD」？**

A: 可以但需要同时改前端 `WMS_API` 模块，把全量读写改为逐条 HTTP 请求。工作量较大，不建议第一版就这么做。

**Q: 后端问「服务器要什么配置」？**

A: 2 核 4G 足以。数据库用 PostgreSQL（JSONB 性能好）。硬盘建议 50GB 以上（考虑备份占用）。

**Q: 后端问「能不能不改 wms-core.js 里的 SUPABASE_URL」？**

A: 必须要改。那个地址是旧的 Supabase 项目。不然后端写的 API 前端根本不会调用。
