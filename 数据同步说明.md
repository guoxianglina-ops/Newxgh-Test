原理很简单，一句话概括：**数据存电脑上，手机和电脑读同一份数据。**

---

## 🏠 打个比方

想象你在用 Excel 记账。如果文件只存在你手机里，电脑就打不开；存电脑里，手机就打不开。我们的做法是：**把 Excel 文件放在电脑的共享文件夹里，手机和电脑都能访问这个文件。**

---

## 🖥️ 技术实现（三步）

### 1. 电脑上运行了一个"数据管家"程序
运行 `server.js`，它做了两件事：
- **存数据**：把生意数据存在电脑文件 `server-data.json` 里
- **接请求**：等着手机和电脑来读写这个文件

### 2. 手机和电脑都打开同一个网址
手机和电脑都访问 `http://172.29.151.59:8899`，因为连的是同一个 WiFi/热点，手机可以访问到电脑。

### 3. 任何操作都自动同步
- 你在手机上新增一条入库单 → 页面自动把数据发给电脑保存
- 你在电脑上刷新 → 页面从电脑读取最新数据
- 反过来也一样

---

## 📱💻 实际操作流程

```
你拿手机扫码 → 打开入库单 → 填数量 → 点保存
                                ↓
                    数据飞到电脑上的 server-data.json
                                ↓
         电脑刷新页面 ← 数据自动显示出来
```

---

## ⚠️ 注意事项

1. **电脑必须开着**，否则手机访问不到地址
2. **手机和电脑必须在同一个网络**（连同一个 WiFi，或者电脑开热点给手机连）
3. **不要改电脑 IP 地址**，目前固定为 `172.29.151.59`，如果换了网络需要更新 IP

---

## 🔧 怎么启动

1. 在 `d:\象过河软件\新系统\` 文件夹空白处按住 Shift + 右键，选"在此处打开 PowerShell 窗口"
2. 输入以下命令并回车：
```
node server.js
```
3. 看到类似下面的提示就成功了：
```
══════════════════════════════════════════
  象过河仓库管理系统 — 后端已启动
  本机访问: http://localhost:8899/
  手机访问: http://192.168.x.x:8899/mobile.html
  ...
══════════════════════════════════════════
```
**不要关掉这个黑色窗口**，关了软件就不能用了。

---

## 🔄 换地方使用

到新的地方（比如十公里外的仓库），只需要：
1. 把整个 `新系统` 文件夹拷过去（U盘或网盘）
2. 新电脑上装好 Node.js（去 nodejs.org 下载安装，一次性的）
3. 新电脑开热点给手机连
4. 运行 `node server.js`，看屏幕上显示的 IP
5. 打开 `qrcode-quick-in.html` 生成二维码（二维码地址会自动用新的 IP）

**就这么简单。不需要改任何代码。**