# 象过河仓库管理系统 V2 — 前后端接口文档

**阅读对象**: 后端开发人员  
**用途**: 了解前端需要的 API、请求参数和返回格式  
**更新日期**: 2026-07-16

---

## 一、通用说明

### 1.1 API 基础信息

- **协议**: HTTP/HTTPS
- **Content-Type**: `application/json`
- **认证**: 通过 HTTP Header 传递用户身份

### 1.2 认证方式

所有接口通过 HTTP Header 传递用户身份：

```
X-User-AccountId: 1
X-User-Name: 张三
X-User-Role: staff
```

> **可用角色**: `staff`（员工）、`auditor`（审核员）、`supervisor`（高级主管）

### 1.3 通用返回格式

```json
{
  "code": 200,
  "message": "success",
  "data": { }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| code | int | 200 成功，4xx 客户端错误，5xx 服务端错误 |
| message | string | 提示信息 |
| data | object/array | 返回数据（可能为 null） |

### 1.4 错误码

| code | 说明 |
|------|------|
| 200 | 成功 |
| 400 | 请求参数有误 |
| 401 | 未登录或登录已过期 |
| 403 | 没有操作权限 |
| 404 | 数据不存在 |
| 409 | 数据冲突（如版本号不匹配） |
| 500 | 服务器内部错误 |

---

## 二、数据实体定义

### 2.1 商品（Goods）

```json
{
  "id": 1,
  "code": "SP001",
  "name": "通用商品A",
  "spec": "标准",
  "model": "GA-001",
  "unit": "个",
  "barcode": "6901234567890",
  "category": "成品",
  "purchPrice": 10.00,
  "costPrice": 9.00,
  "retailPrice": 15.00,
  "industryType": "general",
  "status": "正常",
  "note": "",
  "image": ""
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | int | 是 | 主键，自增 |
| code | string | 是 | 商品编码，如 SP001 |
| name | string | 是 | 商品名称 |
| spec | string | 否 | 规格 |
| model | string | 否 | 型号 |
| unit | string | 否 | 单位 |
| barcode | string | 否 | 条形码 |
| category | string | 否 | 商品类别 |
| purchPrice | number | 否 | 采购价 |
| costPrice | number | 否 | 成本价 |
| retailPrice | number | 否 | 零售价 |
| industryType | string | 是 | 行业类型：general(通用) / food(食品) / apparel(服装) / digital(数码) / building(建材) |
| status | string | 是 | 状态：正常 / 停用 |
| note | string | 否 | 备注 |
| image | string | 否 | 商品图片 URL |
| batchNo | string | 否 | 批号（食品行业） |
| prodDate | string | 否 | 生产日期（食品行业） |
| expiryDate | string | 否 | 有效期至（食品行业） |
| shelfLife | int | 否 | 保质期天数（食品行业） |
| serialNo | string | 否 | 序列号（数码行业） |
| colorGroupId | int | 否 | 颜色组ID（服装行业） |
| sizeGroupId | int | 否 | 尺码组ID（服装行业） |
| material | string | 否 | 材质（建材行业） |
| origin | string | 否 | 产地（建材行业） |
| weight | string | 否 | 重量（建材行业） |
| length | string | 否 | 长度（建材行业） |

---

### 2.2 供应商（Supplier）

```json
{
  "id": 1,
  "code": "GYS001",
  "name": "优质供应商公司",
  "contact": "王经理",
  "tel": "13900001111",
  "addr": "北京市朝阳区",
  "bank": "工商银行",
  "acct": "6222021234567890",
  "note": ""
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | int | 是 | 主键 |
| code | string | 是 | 供应商编号 |
| name | string | 是 | 供应商名称 |
| contact | string | 否 | 联系人 |
| tel | string | 否 | 电话 |
| addr | string | 否 | 地址 |
| bank | string | 否 | 开户银行 |
| acct | string | 否 | 银行账号 |
| note | string | 否 | 备注 |

---

### 2.3 客户（Customer）

```json
{
  "id": 1,
  "code": "KH001",
  "name": "盛世商贸有限公司",
  "contact": "陈老板",
  "tel": "13700001111",
  "addr": "广州市天河区",
  "credit": 50000.00,
  "note": ""
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | int | 是 | 主键 |
| code | string | 是 | 客户编号 |
| name | string | 是 | 客户名称 |
| contact | string | 否 | 联系人 |
| tel | string | 否 | 电话 |
| addr | string | 否 | 地址 |
| credit | number | 否 | 信用额度（元） |
| note | string | 否 | 备注 |

---

### 2.4 仓库（Warehouse）

```json
{
  "id": 1,
  "code": "CK001",
  "name": "总仓库",
  "addr": "公司总部",
  "mgr": "张主管",
  "tel": "13800001111"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | int | 是 | 主键 |
| code | string | 是 | 仓库编号 |
| name | string | 是 | 仓库名称 |
| addr | string | 否 | 地址 |
| mgr | string | 否 | 负责人 |
| tel | string | 否 | 电话 |

---

### 2.5 员工（Staff）

```json
{
  "id": 1,
  "code": "YG001",
  "name": "张三",
  "dept": "采购部",
  "tel": "13600001111",
  "note": ""
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | int | 是 | 主键 |
| code | string | 是 | 员工编号 |
| name | string | 是 | 员工姓名 |
| dept | string | 否 | 所属部门 |
| tel | string | 否 | 电话 |
| note | string | 否 | 备注 |

---

### 2.6 库存（Inventory）

```json
{
  "goodsId": 1,
  "warehouseId": 1,
  "qty": 100,
  "warnQty": 20
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| goodsId | int | 是 | 商品ID |
| warehouseId | int | 是 | 仓库ID |
| qty | number | 是 | 当前库存数量 |
| warnQty | int | 是 | 预警数量，低于此值触发库存预警 |

---

### 2.7 采购订单（PurchaseOrder）

```json
{
  "id": 1,
  "code": "CGDD-20260716-001",
  "date": "2026-07-16",
  "supplierId": 1,
  "staffId": 1,
  "note": "",
  "details": [
    { "goodsId": 1, "qty": 10, "price": 9.50, "amt": 95.00 }
  ],
  "totalAmt": 95.00,
  "status": "已审核",
  "inQty": 0,
  "auditStatus": "已审核",
  "operatorId": "1",
  "auditorId": "0",
  "auditReason": "",
  "rejectedBy": "",
  "cancelledBy": ""
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | int | 是 | 主键 |
| code | string | 是 | 订单编号，格式 CGDD-日期-序号 |
| date | string | 是 | 订单日期，格式 YYYY-MM-DD |
| supplierId | int | 是 | 供应商ID |
| staffId | int | 否 | 制单人（员工ID） |
| note | string | 否 | 备注 |
| details | array | 是 | 订单明细数组 |
| details[].goodsId | int | 是 | 商品ID |
| details[].qty | number | 是 | 数量 |
| details[].price | number | 是 | 单价 |
| details[].amt | number | 是 | 金额（数量×单价） |
| totalAmt | number | 是 | 合计金额 |
| status | string | 是 | 业务状态：草稿 / 已审核 / 部分入库 / 已完成 |
| inQty | number | 是 | 已入库总数量 |
| auditStatus | string | 是 | 审核状态：草稿 / 待审核 / 已审核 / 已驳回 / 已取消 |
| operatorId | string | 否 | 操作员账号ID |
| auditorId | string | 否 | 审核员账号ID |
| auditReason | string | 否 | 驳回原因 |
| rejectedBy | string | 否 | 驳回人 |
| cancelledBy | string | 否 | 取消人 |

---

### 2.8 采购入库单（PurchaseIn）

```json
{
  "id": 1,
  "code": "CGRK-20260716-001",
  "date": "2026-07-16",
  "supplierId": 1,
  "warehouseId": 1,
  "staffId": 3,
  "purchaseOrderId": 1,
  "note": "",
  "details": [
    { "goodsId": 1, "qty": 5, "price": 9.50, "amt": 47.50 }
  ],
  "totalAmt": 47.50,
  "paidAmt": 0,
  "auditStatus": "已审核",
  "operatorId": "1",
  "auditorId": "0",
  "purchaseOrderId": 1
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | int | 是 | 主键 |
| code | string | 是 | 单号，格式 CGRK-日期-序号 |
| date | string | 是 | 日期 |
| supplierId | int | 是 | 供应商ID |
| warehouseId | int | 是 | 入库仓库ID |
| staffId | int | 否 | 制单人 |
| purchaseOrderId | int | 否 | 关联的采购订单ID（可为null，表示独立入库） |
| note | string | 否 | 备注 |
| details | array | 是 | 入库明细 |
| totalAmt | number | 是 | 合计金额 |
| paidAmt | number | 否 | 已付金额 |
| auditStatus | string | 是 | 审核状态 |
| operatorId | string | 否 | 操作员 |
| auditorId | string | 否 | 审核员 |

---

### 2.9 采购退货单（PurchaseReturn）

```json
{
  "id": 1,
  "code": "CGTH-20260716-001",
  "date": "2026-07-16",
  "supplierId": 1,
  "warehouseId": 1,
  "staffId": 3,
  "purchaseOrderId": 1,
  "details": [
    { "goodsId": 1, "qty": 1, "price": 9.50, "amt": 9.50 }
  ],
  "totalAmt": 9.50,
  "auditStatus": "已审核",
  "operatorId": "1",
  "auditorId": "0"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | int | 是 | 主键 |
| code | string | 是 | 单号，格式 CGTH-日期-序号 |
| date | string | 是 | 日期 |
| supplierId | int | 是 | 供应商ID |
| warehouseId | int | 是 | 退货仓库ID |
| staffId | int | 否 | 制单人 |
| purchaseOrderId | int | 否 | 关联的采购订单ID |
| details | array | 是 | 退货明细 |
| totalAmt | number | 是 | 合计金额 |
| auditStatus | string | 是 | 审核状态 |

---

### 2.10 采购付款单（PurchasePayment）

```json
{
  "id": 1,
  "code": "FKJS-20260716-001",
  "date": "2026-07-16",
  "supplierId": 1,
  "amount": 100.00,
  "payMethod": "银行转账",
  "bankAcct": "6222021234567890",
  "note": ""
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | int | 是 | 主键 |
| code | string | 是 | 单号 |
| date | string | 是 | 日期 |
| supplierId | int | 是 | 供应商ID |
| amount | number | 是 | 付款金额 |
| payMethod | string | 否 | 付款方式 |
| bankAcct | string | 否 | 银行账号 |
| note | string | 否 | 备注 |

---

### 2.11 销售订单（SalesOrder）

```json
{
  "id": 1,
  "code": "XSDD-20260716-001",
  "date": "2026-07-16",
  "customerId": 1,
  "staffId": 2,
  "note": "",
  "details": [
    { "goodsId": 1, "qty": 5, "price": 15.00, "amt": 75.00 }
  ],
  "totalAmt": 75.00,
  "outQty": 0,
  "auditStatus": "已审核",
  "operatorId": "2",
  "auditorId": "0"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | int | 是 | 主键 |
| code | string | 是 | 单号，格式 XSDD-日期-序号 |
| date | string | 是 | 日期 |
| customerId | int | 是 | 客户ID |
| staffId | int | 否 | 制单人 |
| note | string | 否 | 备注 |
| details | array | 是 | 订单明细 |
| totalAmt | number | 是 | 合计金额 |
| outQty | number | 是 | 已出库数量 |
| auditStatus | string | 是 | 审核状态 |

---

### 2.12 销售出库单（SalesOut）

```json
{
  "id": 1,
  "code": "XSCK-20260716-001",
  "date": "2026-07-16",
  "customerId": 1,
  "warehouseId": 1,
  "staffId": 2,
  "salesOrderId": 1,
  "details": [
    { "goodsId": 1, "qty": 3, "price": 15.00, "amt": 45.00 }
  ],
  "totalAmt": 45.00,
  "receivedAmt": 0,
  "auditStatus": "已审核",
  "operatorId": "2",
  "auditorId": "0"
}
```

---

### 2.13 销售退货单（SalesReturn）

```json
{
  "id": 1,
  "code": "XSTH-20260716-001",
  "date": "2026-07-16",
  "customerId": 1,
  "warehouseId": 1,
  "details": [
    { "goodsId": 1, "qty": 1, "price": 15.00, "amt": 15.00 }
  ],
  "totalAmt": 15.00,
  "auditStatus": "已审核"
}
```

---

### 2.14 销售收款单（SalesReceipt）

```json
{
  "id": 1,
  "code": "SKJS-20260716-001",
  "date": "2026-07-16",
  "customerId": 1,
  "amount": 200.00,
  "method": "银行转账",
  "note": ""
}
```

---

### 2.15 仓库盘点单（CheckOrder）

```json
{
  "id": 1,
  "code": "PDD-20260716-001",
  "date": "2026-07-16",
  "warehouseId": 1,
  "details": [
    { "goodsId": 1, "bookQty": 100, "actualQty": 98, "diff": -2 }
  ],
  "auditStatus": "已审核"
}
```

---

### 2.16 库存调拨单（Transfer）

```json
{
  "id": 1,
  "code": "DB-20260716-001",
  "date": "2026-07-16",
  "fromWarehouseId": 1,
  "toWarehouseId": 2,
  "details": [
    { "goodsId": 1, "qty": 10 }
  ],
  "auditStatus": "已审核"
}
```

---

### 2.17 收入记录（IncomeRecord）

```json
{
  "id": 1,
  "code": "SR-001",
  "date": "2026-07-16",
  "acctName": "主营业务收入",
  "amount": 5000.00,
  "source": "销售货款",
  "note": ""
}
```

---

### 2.18 费用支出（ExpenseRecord）

```json
{
  "id": 1,
  "code": "ZC-001",
  "date": "2026-07-16",
  "acctName": "办公费用",
  "amount": 200.00,
  "type": "管理费用",
  "note": ""
}
```

---

### 2.19 其他实体

以下实体的具体字段请参考现有系统数据结构：

- **会计科目（Account）**: id, code, name, type, bal
- **计量单位（Unit）**: id, name
- **商品类别（Category）**: id, pid, name
- **颜色组（ColorGroup）**: id, name, colors[]
- **尺码组（SizeGroup）**: id, name, sizes[]
- **BOM物料清单（BOM）**: id, code, parentGoodsId, childGoodsId, qty, unit
- **生产计划（ProductionPlan）**: id, code, goodsId, qty, date, note, status
- **加工单（ProductionOrder）**: id, code, goodsId, qty, date, note, status
- **生产领料（ProductionPick）**: id, code, goodsId, qty, warehouseId, date
- **生产入库（ProductionIn）**: id, code, goodsId, qty, warehouseId, date
- **生产退料（ProductionReturn）**: id, code, goodsId, qty, warehouseId, date
- **报价单（Quotation）**: id, code, customerId, goodsId, price, date
- **会员（Member）**: id, name, phone, level, points, balance, note
- **POS收银（POSOrder）**: id, code, goodsId, qty, price, date
- **发票（Invoice）**: id, code, type, amount, date
- **工程项目（Project）**: id, name, progress, spent, status, note
- **租赁管理（Rental）**: id, name, customerId, startDate, endDate, amount
- **维修管理（Repair）**: id, name, goodsId, status, note
- **返利提成（Rebate）**: id, name, type, amount, status
- **审核日志（AuditLog）**: id, type, recordId, recordCode, action, userId, userName, time
- **消息通知（Notification）**: id, type, recordId, recordCode, action, newStatus, staffId, staffName, userId, time, read

---

## 三、API 接口列表

### 3.1 数据操作接口（共用）

后端需要实现一个统一的**单表 JSONB 存取接口**，所有业务数据存储在一张表的单行 JSONB 列中。

#### 3.1.1 获取全部数据

```
GET /api/data
```

**请求参数**：无

**返回示例**：
```json
{
  "code": 200,
  "data": {
    "version": 5,
    "goods": [ ... ],
    "suppliers": [ ... ],
    "customers": [ ... ],
    "warehouses": [ ... ],
    "staff": [ ... ],
    "accounts": [ ... ],
    "units": [ ... ],
    "categories": [ ... ],
    "colorGroups": [ ... ],
    "sizeGroups": [ ... ],
    "inventory": [ ... ],
    "purchaseOrders": [ ... ],
    "purchaseIn": [ ... ],
    "purchaseReturn": [ ... ],
    "purchasePayments": [ ... ],
    "salesOrders": [ ... ],
    "salesOut": [ ... ],
    "salesReturn": [ ... ],
    "salesReceipts": [ ... ],
    "checkOrders": [ ... ],
    "checkProfit": [ ... ],
    "checkLoss": [ ... ],
    "transfers": [ ... ],
    "incomeRecords": [ ... ],
    "expenseRecords": [ ... ],
    "boms": [ ... ],
    "productionPlans": [ ... ],
    "productionOrders": [ ... ],
    "productionPicks": [ ... ],
    "productionIn": [ ... ],
    "productionReturns": [ ... ],
    "quotations": [ ... ],
    "members": [ ... ],
    "memberRecharges": [ ... ],
    "posOrders": [ ... ],
    "invoices": [ ... ],
    "projects": [ ... ],
    "rentals": [ ... ],
    "repairs": [ ... ],
    "rebates": [ ... ],
    "stockFlows": [ ... ],
    "auditLogs": [ ... ],
    "notifications": [ ... ],
    "redDots": [ ... ],
    "completedBatches": [ ... ],
    "permissions": { ... },
    "nextIds": { ... }
  }
}
```

**说明**：返回整个数据库的全部数据。前端通过 `WMS_API.init()` 调用此接口拉取初始数据。

---

#### 3.1.2 保存全部数据（乐观锁）

```
POST /api/data
```

**请求 Body**：
```json
{
  "version": 5,
  "data": {
    "goods": [ ... ],
    "suppliers": [ ... ],
    ...
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| version | int | 客户端当前持有的版本号 |
| data | object | 完整的数据库 JSON 对象 |

**返回**：
```json
{
  "code": 200,
  "message": "success",
  "data": { "version": 6 }
}
```

**冲突时返回**（version 不匹配）：
```json
{
  "code": 409,
  "message": "版本冲突，请先拉取最新数据",
  "data": {
    "serverVersion": 7,
    "serverData": { ... }
  }
}
```

**说明**：
- 使用**乐观锁**机制防止数据覆盖
- 客户端版本号必须与服务端一致才能写入
- 写入成功后服务端版本号 +1
- 冲突时返回服务端最新版本号和数据，客户端自行合并后重试

---

### 3.2 ID 预分配接口（并发安全）

#### 3.2.1 批量分配 ID

```
POST /api/id/allocate
```

**请求 Body**：
```json
{
  "keys": [
    { "key": "purchaseOrder", "count": 50 },
    { "key": "goods", "count": 20 }
  ]
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| keys[] | array | 需要分配ID的实体列表 |
| keys[].key | string | 实体标识，如 purchaseOrder、goods、supplier 等 |
| keys[].count | int | 预分配数量 |

**返回**：
```json
{
  "code": 200,
  "data": {
    "allocations": [
      { "key": "purchaseOrder", "startId": 51, "count": 50 },
      { "key": "goods", "startId": 21, "count": 20 }
    ]
  }
}
```

**说明**：
- 使用数据库**行锁**或**原子操作**确保并发安全
- 每个客户端预取一段 ID 号段，在自己号段内自行分配
- 号段用完后再请求新的
- 需要预分配的 key 包括：

```
goods, supplier, customer, warehouse, staff, account, unit, category,
colorGroup, sizeGroup, notification, auditLog,
purchaseOrder, purchaseIn, purchaseReturn, purchasePayment,
salesOrder, salesOut, salesReturn, salesReceipt,
checkOrder, checkProfit, checkLoss, transfer,
incomeRecord, expenseRecord,
bom, productionPlan, productionOrder, productionPick, productionIn, productionReturn,
quotation, member, memberRecharge, posOrder, invoice, project, rental, repair, rebate,
stockFlow, completedBatch
```

---

### 3.3 审核操作接口

#### 3.3.1 提交审核

```
POST /api/audit/submit
```

**请求 Body**：
```json
{
  "type": "purchaseOrder",
  "id": 1
}
```

**返回**：
```json
{
  "code": 200,
  "message": "已提交审核"
}
```

**说明**：
- 将指定记录的 `auditStatus` 改为 `待审核`
- 记录 `operatorId`
- 创建红点通知给审核员
- 仅 `staff` 角色可操作
- `type` 可选值见 AUDITABLE 列表

---

#### 3.3.2 审核通过

```
POST /api/audit/approve
```

**请求 Body**：
```json
{
  "type": "purchaseOrder",
  "id": 1
}
```

**返回**：
```json
{
  "code": 200,
  "message": "已审核通过"
}
```

**说明**：
- 仅 `auditor` 或 `supervisor` 角色可操作
- 采购入库审核通过时自动更新库存、创建批次记录
- 销售出库审核通过时自动扣减库存

---

#### 3.3.3 驳回

```
POST /api/audit/reject
```

**请求 Body**：
```json
{
  "type": "purchaseOrder",
  "id": 1,
  "reason": "单价异常，请核实"
}
```

**返回**：
```json
{
  "code": 200,
  "message": "已驳回"
}
```

---

#### 3.3.4 取消/撤回

```
POST /api/audit/cancel
```

**请求 Body**：
```json
{
  "type": "purchaseOrder",
  "id": 1
}
```

**返回**：
```json
{
  "code": 200,
  "message": "已取消"
}
```

**说明**：
- 仅 `supervisor` 角色可操作
- `auditStatus` 改为 `已取消`

---

### 3.4 单条记录操作接口

#### 3.4.1 创建商品

```
POST /api/goods
```

**请求 Body**：
```json
{
  "name": "新商品",
  "spec": "标准",
  "model": "X-001",
  "unit": "个",
  "category": "成品",
  "barcode": "",
  "purchPrice": 10.00,
  "costPrice": 8.00,
  "retailPrice": 15.00,
  "industryType": "general",
  "warehouseId": 1,
  "warnQty": 10,
  "note": ""
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| name | string | 是 | 商品名称 |
| spec | string | 否 | 规格 |
| model | string | 否 | 型号 |
| unit | string | 否 | 单位 |
| category | string | 否 | 类别 |
| barcode | string | 否 | 条码 |
| purchPrice | number | 否 | 采购价 |
| costPrice | number | 否 | 成本价 |
| retailPrice | number | 否 | 零售价 |
| industryType | string | 是 | 行业类型 |
| warehouseId | int | 否 | 初始仓库ID |
| warnQty | int | 否 | 预警数量 |
| note | string | 否 | 备注 |
| image | string | 否 | 商品图片URL |

**返回**：
```json
{
  "code": 200,
  "message": "商品已创建",
  "data": { "id": 4, "code": "SP004" }
}
```

---

#### 3.4.2 更新商品

```
PUT /api/goods/:id
```

**请求 Body**：同创建商品

**返回**：
```json
{
  "code": 200,
  "message": "商品已更新"
}
```

---

#### 3.4.3 删除商品

```
DELETE /api/goods/:id
```

**返回**：
```json
{
  "code": 200,
  "message": "商品已删除"
}
```

---

#### 3.4.4 其他实体的 CRUD 接口

以下接口遵循同样的 RESTful 风格：

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/suppliers | 获取供应商列表 |
| POST | /api/suppliers | 创建供应商 |
| PUT | /api/suppliers/:id | 更新供应商 |
| DELETE | /api/suppliers/:id | 删除供应商 |
| GET | /api/customers | 获取客户列表 |
| POST | /api/customers | 创建客户 |
| PUT | /api/customers/:id | 更新客户 |
| DELETE | /api/customers/:id | 删除客户 |
| GET | /api/warehouses | 获取仓库列表 |
| POST | /api/warehouses | 创建仓库 |
| DELETE | /api/warehouses/:id | 删除仓库 |
| GET | /api/staff | 获取员工列表 |
| POST | /api/staff | 创建员工 |
| DELETE | /api/staff/:id | 删除员工 |
| GET | /api/accounts | 获取会计科目列表 |
| POST | /api/accounts | 创建会计科目 |
| DELETE | /api/accounts/:id | 删除会计科目 |
| GET | /api/units | 获取单位列表 |
| POST | /api/units | 创建单位 |
| DELETE | /api/units/:id | 删除单位 |
| GET | /api/categories | 获取类别列表 |
| POST | /api/categories | 创建类别 |
| DELETE | /api/categories/:id | 删除类别 |
| GET | /api/color-groups | 获取颜色组列表 |
| POST | /api/color-groups | 创建颜色组 |
| PUT | /api/color-groups/:id | 更新颜色组 |
| DELETE | /api/color-groups/:id | 删除颜色组 |
| GET | /api/size-groups | 获取尺码组列表 |
| POST | /api/size-groups | 创建尺码组 |
| PUT | /api/size-groups/:id | 更新尺码组 |
| DELETE | /api/size-groups/:id | 删除尺码组 |
| GET | /api/inventory | 获取库存列表 |
| PUT | /api/inventory/warn | 更新库存预警数量 |

---

### 3.5 业务单据接口

#### 3.5.1 采购订单系列

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/purchase-orders | 获取采购订单列表 |
| POST | /api/purchase-orders | 创建采购订单 |
| PUT | /api/purchase-orders/:id | 修改采购订单 |
| DELETE | /api/purchase-orders/:id | 删除采购订单 |

#### 3.5.2 采购入库系列

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/purchase-in | 获取采购入库列表 |
| POST | /api/purchase-in | 创建采购入库单 |
| PUT | /api/purchase-in/:id | 修改采购入库单 |
| DELETE | /api/purchase-in/:id | 删除采购入库单 |

#### 3.5.3 其他业务单据

遵循同样的 CRUD 模式：

| 单据 | 路径前缀 | 说明 |
|------|---------|------|
| 采购退货 | /api/purchase-returns | |
| 采购付款 | /api/purchase-payments | |
| 销售订单 | /api/sales-orders | |
| 销售出库 | /api/sales-out | |
| 销售退货 | /api/sales-returns | |
| 销售收款 | /api/sales-receipts | |
| 仓库盘点 | /api/check-orders | |
| 库存调拨 | /api/transfers | |
| 收入记录 | /api/income-records | |
| 费用支出 | /api/expense-records | |
| BOM清单 | /api/boms | |
| 生产计划 | /api/production-plans | |
| 加工单 | /api/production-orders | |
| 生产领料 | /api/production-picks | |
| 生产入库 | /api/production-in | |
| 生产退料 | /api/production-returns | |
| 报价单 | /api/quotations | |
| 会员 | /api/members | |
| POS订单 | /api/pos-orders | |
| 发票 | /api/invoices | |
| 工程项目 | /api/projects | |
| 租赁 | /api/rentals | |
| 维修 | /api/repairs | |
| 返利提成 | /api/rebates | |

---

## 四、AUDITABLE 审核类型表

系统支持以下类型的审核流程：

| type | 实体列表字段 | 中文名称 | 英文名称 |
|------|------------|---------|---------|
| purchaseOrder | purchaseOrders | 采购订单 | PurchaseOrder |
| purchaseIn | purchaseIn | 采购入库 | PurchaseIn |
| purchaseReturn | purchaseReturn | 采购退货 | PurchaseReturn |
| purchasePay | purchasePayments | 采购付款 | PurchasePayment |
| salesOrder | salesOrders | 销售订单 | SalesOrder |
| salesOut | salesOut | 销售出库 | SalesOut |
| salesReturn | salesReturn | 销售退货 | SalesReturn |
| salesReceive | salesReceipts | 销售收款 | SalesReceipt |
| checkOrder | checkOrders | 仓库盘点 | CheckOrder |
| transfer | transfers | 库存调拨 | Transfer |
| productionPlan | productionPlans | 生产计划 | ProductionPlan |
| productionOrder | productionOrders | 加工单 | ProductionOrder |
| productionPick | productionPicks | 生产领料 | ProductionPick |

---

## 五、单号前缀规则

| 单据类型 | 前缀 | 格式示例 |
|---------|------|---------|
| 采购订单 | CGDD | CGDD-20260716-001 |
| 采购入库 | CGRK | CGRK-20260716-001 |
| 采购退货 | CGTH | CGTH-20260716-001 |
| 采购付款 | FKJS | FKJS-20260716-001 |
| 销售订单 | XSDD | XSDD-20260716-001 |
| 销售出库 | XSCK | XSCK-20260716-001 |
| 销售退货 | XSTH | XSTH-20260716-001 |
| 销售收款 | SKJS | SKJS-20260716-001 |
| 仓库盘点 | PDD | PDD-20260716-001 |
| 库存调拨 | DB | DB-20260716-001 |
| 生产计划 | SCJH | SCJH-20260716-001 |
| 加工单 | JGD | JGD-20260716-001 |
| 生产领料 | SCLL | SCLL-20260716-001 |
| 生产入库 | SCRK | SCRK-20260716-001 |
| 生产退料 | SCTL | SCTL-20260716-001 |
| 收入 | SR | SR-20260716-001 |
| 支出 | ZC | ZC-20260716-001 |
| BOM | BOM | BOM-001 |
| 商品 | SP | SP004 |
| 供应商 | GYS | GYS003 |
| 客户 | KH | KH003 |
| 仓库 | CK | CK003 |
| 员工 | YG | YG004 |

---

## 六、审核状态流转

```
草稿 ──→ 待审核 ──→ 已审核
  │         │
  │         ├──→ 已驳回 ──→ (修改后重新提交) ──→ 待审核
  │         │
  ├──→ 已取消 (仅主管)
  │
  └──→ 删除
```

---

## 七、后端实现建议

### 7.1 数据库选型

建议使用 **PostgreSQL**，数据结构为一表一行 JSONB：

```sql
CREATE TABLE app_data (
  id INTEGER PRIMARY KEY DEFAULT 1,
  data JSONB NOT NULL DEFAULT '{}',
  version INTEGER DEFAULT 0,
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE id_counters (
  key TEXT PRIMARY KEY,
  current_value BIGINT NOT NULL DEFAULT 0
);
```

### 7.2 并发控制

- **数据保存**：使用 `version` 字段做乐观锁
- **ID 分配**：使用 `SELECT ... FOR UPDATE` 行锁确保 ID 原子递增

### 7.3 必须实现的核心接口优先级

| 优先级 | 接口 | 原因 |
|--------|------|------|
| P0 | GET /api/data | 前端启动必须拉取全量数据 |
| P0 | POST /api/data | 所有写操作最终走此接口 |
| P1 | POST /api/id/allocate | 多设备并发下单必须 |
| P2 | POST /api/audit/* | 审核流程 |
| P3 | RESTful CRUD | 逐条操作（可先走全量保存兜底） |

---

## 八、附录：前端现有 Supabase 数据结构

当前系统使用 Supabase PostgreSQL，数据通过 REST API + RPC 函数操作。后端如果搭建自有服务，只需替换前端 `WMS_API` 模块中 `SUPABASE_URL` 和 `SUPABASE_KEY` 变量，改成自己的 API 地址即可。

前端相关代码位置：[wms-core.js](wms-core.js) 第 498 行起 `WMS_API` 模块。
