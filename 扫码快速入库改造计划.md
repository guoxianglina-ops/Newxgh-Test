# 扫码快速入库/出库 — 改造计划（终版）

## 一、URL 参数协议

二维码编码为 `mobile.html` 的 URL，参数如下：

```
mobile.html?action={quick-in|quick-out}&goodsId=X&qty=N&qtyLock=1&price=P&priceLock=1&whId=W&whLock=1&supId=S&supLock=1&custId=C&custLock=1
```

| 参数 | 必填 | 说明 |
|------|------|------|
| `action` | ✓ | `quick-in` 入库 / `quick-out` 出库 |
| `goodsId` | ✓ | 商品 ID |
| `qty` | 可选 | 入库/出库数量，不填则预填1 |
| `qtyLock` | 可选 | `1`=数量锁定，扫码后不可改 |
| `price` | 可选 | 入库/出库单价，不填则留空 |
| `priceLock` | 可选 | `1`=单价锁定，扫码后不可改 |
| `whId` | 可选 | 仓库 ID，不填则留空 |
| `whLock` | 可选 | `1`=仓库锁定，扫码后不可改 |
| `supId` | 可选 | 供应商 ID（仅入库有效） |
| `supLock` | 可选 | `1`=供应商锁定，扫码后不可改（仅入库有效） |
| `custId` | 可选 | 客户 ID（仅出库有效） |
| `custLock` | 可选 | `1`=客户锁定，扫码后不可改（仅出库有效） |

> **URL 最长**：含全部参数时约 150 字符，远低于二维码 200 字符安全上限。

## 状态：✅ 全部已完成

## 二、二维码生成器改造 (`qrcode-quick-in.html`)

### 2.1 全局配置区（表单上方）

```
┌─────────────────────────────────────────┐
│ 📦 二维码生成器                          │
│                                          │
│ 类型: [○ 采购入库] [○ 销售出库]          │
│ 仓库: [下拉选择]                          │
│ 供应商: [下拉选择] [□ 锁定(扫码后不可改)]  │
│ 数量: [______] (默认1)                    │
│ 单价: [______] (默认0，留空需手动填)       │
└─────────────────────────────────────────┘
```

- **供应商逻辑**：
  - 不选 → 方案A：扫码后手动选
  - 选了但未勾"锁定" → 方案B：预填但允许改
  - 选了且勾"锁定" → 方案B锁定：禁止修改
- **商品默认供应商**：如果商品在数据库里有绑定供应商（需新增字段或使用最近采购的供应商），自动填入；否则留空

### 2.2 批量生成模式

```
┌─────────────────────────────────────────┐
│ 🔄 批量生成                              │
│ 起始商品ID: [___]  结束商品ID: [___]      │
│ [🔄 生成全部]  [🖨 全部打印]              │
└─────────────────────────────────────────┘
```

所有商品共用全局配置。点击"生成全部"后下方出现每个商品的二维码卡片。

### 2.3 逐条定制模式

```
┌─────────────────────────────────────────┐
│ ✏️ 逐条定制                              │
│ 继承全局配置 ── 可单独覆盖 ──              │
│ 商品: [下拉选择] 数量: [___] 单价: [___]   │
│ 仓库: [下拉] 供应商: [下拉]                │
│ [➕ 添加到列表]                           │
└─────────────────────────────────────────┘
```

添加到列表后出现在下方二维码区域。可删除单条。已添加到列表的也为最终二维码集合。

### 2.4 二维码展示区

每张卡片：二维码图片 + 商品名称 + 预填参数摘要 + 🖨打印按钮 + ❌删除按钮（逐条的才可删）。

## 三、手机端改造 (`wms-core.js`)

### 3.1 `handleQuickActions()` 参数扩展

```js
function handleQuickActions(){
  解析: action, goodsId, qty, price, whId, supId
  清理URL参数
  调用重复扫码检查 → 放行则继续
  登录状态检查 → 已登录直接跳；未登录等登录后再处理
  
  if(action==='quick-in'){
    nav('purchase-in','采购入库');
    setTimeout(()=>addPI_QR(null, goodsId, qty, price, whId, supId), 400);
  } else {
    nav('sales-out','销售出库');
    setTimeout(()=>addSOut_QR(null, goodsId, qty, price, whId), 400);
  }
}
```

### 3.2 `addPI_QR()` — 入库扫码专用（新函数）

```js
function addPI_QR(orderId, goodsId, qty, price, whId, supId){
  // 1. 如果商品有默认供应商且未传supId → 自动填入
  // 2. 如果 supId 传入 → 供应商锁定(disabled)
  // 3. 如果 whId 传入 → 仓库预选
  // 4. 如果 qty/price 传入 → 对应input预填
  // 5. 弹窗加"⚡一键创建"按钮 → 跳过确认直接savePI_QR()
  // 6. 弹窗关闭时清理URL已做过
}
```

### 3.3 `addSOut_QR()` — 出库扫码专用（新函数）

同理，复用 `addSOut()` 的逻辑，增加 qty/price/whId 预填和一键创建。

### 3.4 `savePI_QR()` / `saveSOut_QR()` — 一键创建

```js
function savePI_QR(){
  // 校验必填项（供应商必填，否则toast提示）
  // 直接调用现有savePI()的核心逻辑
  // 保存成功后toast "已创建入库单 {code}" + 记录扫码日志
  // clsModal() 关闭弹窗
}
```

### 3.5 重复扫码确认

```
扫码日志结构（localStorage）:
{
  "2026-07-18": {
    "12": 3,    // goodsId: 通过扫码创建的订单数
    "15": 1
  }
}

今日不再提醒标记（localStorage）:
wms_qr_noremind_12_2026-07-18 = true
```

流程：
1. 扫码 → 解析 goodsId
2. 查今日日志 → 已有 N 条
3. 查"今日不再提醒" → 有则跳过确认
4. 弹窗："您今日已通过扫描该码创建 {N} 条订单，是否继续？"
   [继续创建] [今日不再提醒] [取消]
5. 创建成功后 → 日志 +1

### 3.6 商品默认供应商（两者结合）

**规则**：
1. 商品表新增可选字段 `defaultSupplierId`
2. 生成二维码时：商品有 `defaultSupplierId` → 自动填入供应商
3. 商品没有绑定 → 查找该商品最近一次采购入库对应的供应商 → 自动填入
4. 两种都找不到 → 供应商留空

**实现**：
- `gGoodsDefaultSupplier(goodsId)` → 先查 `goods.defaultSupplierId`，再查最近采购入库 → 返回 supplierId 或 null
- 二维码生成器页面调用此函数获取推荐供应商

### 3.7 一键创建逻辑

当以下字段 **全部** 由二维码传入且非空时，显示"⚡一键创建"按钮：
- goodsId ✓（必传）
- qty > 0
- price > 0
- whId 有效
- supId 有效（入库）/ customerId 有效（出库 —— 出库无供应商概念）

一键创建按钮点击 = `savePI_QR()` / `saveSOut_QR()` 直接保存，跳过弹窗确认。

如果缺少任一必填 → 弹窗正常显示，字段预填但需要用户手动补全+点击保存。

## 四、改动文件清单

| 文件 | 改动量 | 内容 |
|------|--------|------|
| `qrcode-quick-in.html` | 重写 | 全局配置+批量+逐条+供应商锁定+出库支持 |
| `wms-core.js` | ~150行 | `handleQuickActions()` 扩展；`addPI_QR()`/`addSOut_QR()`/`savePI_QR()`/`saveSOut_QR()`；重复扫码确认；扫码日志 |
| `mobile.html` | 不改 | 已在 `initApp` 中调用 `handleQuickActions` |
| `index.html` | 不改 | 电脑版不涉及扫码 |
| `deploy-netlify/` | 同步 | |

## 五、二维码 URL 示例

```
# 基础入库（仅商品）
https://test-project-xgh.pages.dev/mobile.html?action=quick-in&goodsId=12

# 全参数入库
https://test-project-xgh.pages.dev/mobile.html?action=quick-in&goodsId=12&qty=5&price=23.5&whId=1&supId=3

# 全参数出库
https://test-project-xgh.pages.dev/mobile.html?action=quick-out&goodsId=12&qty=2&price=50&whId=1
```

最长全参数 URL 约 150 字符，远低于二维码 200 字符安全上限。

## 六、实现完成情况

| 模块 | 文件 | 状态 |
|------|------|------|
| 二维码生成器 | `qrcode-quick-in.html` | ✅ 已完成 — 全局配置 + 批量 + 逐条 + 出入库双模式 + 供应商锁定 + 删除/打印 |
| 扫码处理 | `wms-core.js` → `handleQuickActions()` | ✅ 已完成 — 解析 qty/price/whId/supId/custId + 暂存参数 |
| 扫码入库 | `wms-core.js` → `addPI_QR()` | ✅ 已完成 — 预填所有字段 + 供应商锁定 + 一键创建 |
| 扫码出库 | `wms-core.js` → `addSOut_QR()` | ✅ 已完成 — 预填所有字段 + 客户锁定 + 一键创建 |
| 重复扫码确认 | `wms-core.js` → `qrCheckDup()`/`qrLogScan()` | ✅ 已完成 — 今日计数 + 弹窗确认 + 今日不再提醒 |
| 商品默认供应商 | `wms-core.js` → `gGoodsDefaultSupplier()` | ✅ 已完成 — 商品绑定 > 最近采购推断 |
| 一键创建 | `wms-core.js` → `savePI_QR()`/`saveSOut_QR()` | ✅ 已完成 — 全部预填时直接保存 |
| 手机版入口 | `mobile.html` | ✅ 无需改动 — 已在 `initApp` 中调用 `handleQuickActions` |

**二维码生成器入口**：`https://test-project-xgh.pages.dev/qrcode-quick-in.html`
