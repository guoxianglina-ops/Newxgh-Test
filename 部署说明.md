# 部署说明（象过河 WMS）

> 最后更新：2026-09-17

## 一、线上是怎么发布的

**重要：这个 Cloudflare Pages 项目是「直传模式」，没有连 Git。**
所以 `git push` **不会**更新线上，必须用命令手动发布。

### 发布命令

```powershell
cd D:\ai-mbti
npx wrangler pages deploy . --project-name=test-project-xgh --branch=main --commit-dirty=true --cwd "D:\象过河软件\新系统\deploy-netlify"
```

两个容易踩的点：

1. **`--branch=main` 不能省。** 这个项目的生产分支就是 `main`。省掉会发成「预览部署」，线上还是旧的。
2. **`--cwd` 必须指向 `deploy-netlify`。** `wrangler pages deploy` 是在**当前工作目录**下找 `functions/` 目录的，
   不指过去的话，`functions/api/chat.js` 这个转发接口不会被部署，AI 助手就会退化成纯本地检索。

发布成功后命令行会打印一个预览地址（形如 `https://xxxx.test-project-xgh.pages.dev`），
生产地址始终是 https://test-project-xgh.pages.dev

### 只读排查命令

```powershell
npx wrangler whoami
npx wrangler pages project list
npx wrangler pages deployment list --project-name=test-project-xgh
```

---

## 二、改完代码要注意的同步

根目录和 `deploy-netlify/` 是**两份**文件，线上只认 `deploy-netlify/` 那份。

改完下面这些文件后，记得同步一份过去：

| 根目录 | 需要同步到 |
|---|---|
| `wms-core.js` | `deploy-netlify/wms-core.js` |
| `ai-assistant.js` | `deploy-netlify/ai-assistant.js` |
| `index.html` | `deploy-netlify/index.html` |
| `mobile.html` | `deploy-netlify/mobile.html` |
| `qrcode-quick-in.html` | `deploy-netlify/qrcode-quick-in.html` |

**不同步的后果**：比如 HTML 里删掉了某个 script 标签，但线上那份 JS 还在等那个库 —— 功能会直接坏掉。

---

## 三、AI 助手的 API Key 放在哪

**不再放在代码里。** 现在的结构是：

```
浏览器  ──POST /api/chat──▶  Cloudflare Pages Function  ──带 Key──▶  DeepSeek
                              (functions/api/chat.js)
                              Key 来自环境变量 DEEPSEEK_API_KEY
```

- 前端 `ai-assistant.js` 里 `useProxy: true`，只调 `/api/chat`，**不带任何密钥**
- Key 存在 Cloudflare：Workers & Pages → test-project-xgh → Settings → **Variables and secrets**
  （环境选 Production），变量名 `DEEPSEEK_API_KEY`，类型 Secret
- 改完环境变量**必须重新部署一次**才生效

### 本地调试怎么办

- 用 `node server.js` 起本地服务时，`/api/chat` 不存在，AI 助手会自动退回**本地知识库检索模式**
  （能答操作问题、能查业务数据，但不能通过对话创建订单）
- 如果本地也要连大模型：把 `useProxy` 改成 `false`，并临时填 `apiKey`
  —— **切记不要把真实 Key 提交到仓库**

---

## 四、如何自检发布是否成功

Cloudflare Pages 对「不存在的路径」会兜底返回首页（HTTP 200），
所以**不能用「能不能打开」判断文件在不在**。

可靠做法是在浏览器控制台里读内容再断言，例如：

```js
// 检查转发接口是否部署成功（应返回 200 且是 JSON）
fetch('/api/chat', { method:'POST', headers:{'Content-Type':'application/json'},
  body: JSON.stringify({ model:'deepseek-chat', messages:[{role:'user',content:'hi'}], max_tokens:10 })
}).then(r => r.text()).then(console.log);

// 检查某个文件是否真的在线（不要只看状态码）
fetch('/ai-assistant.js', { cache:'no-store' }).then(r => r.text())
  .then(t => console.log('含真实密钥:', /sk-[A-Za-z0-9]{20,}/.test(t), '行数:', t.length));
```

拿一个**肯定不存在**的路径（如 `/zzz-not-exist.js`）作对照，
如果它返回的是首页 HTML，说明这个「兜底」行为存在，上面的判断方法才成立。
